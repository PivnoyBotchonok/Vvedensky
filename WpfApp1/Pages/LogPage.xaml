<Page x:Class="WpfApp1.Pages.LogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LogPage" Background="White">
    <Grid>
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="250">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Content="Вход" Grid.Row="0" HorizontalAlignment="Center"/>
            <Label Content="Логин" Grid.Row="1" HorizontalAlignment="Center"/>
            <TextBox x:Name="Login" Grid.Row="2" Margin="0,5"/>
            <Label Content="Пароль" Grid.Row="3" HorizontalAlignment="Center"/>
            <TextBox x:Name="Password" Grid.Row="4" Margin="0,5"/>
            <Button x:Name="EntryBut" Content="Вход" Grid.Row="5" Margin="0,5" Click="EntryBut_Click"/>
            <Button x:Name="QrBut" Content="Qr-код" Grid.Row="6" Margin="0,5" Click="QrBut_Click"/>
            <Button x:Name="RegBut" Content="Регистрация" Grid.Row="7" Margin="0,5" Click="RegBut_Click"/>
        </Grid>
    </Grid>
</Page>